[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "agentic-ai"
version = "1.0"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["config*", "core*", "rag*", "utils*", "app_mcp*","frontend*"]